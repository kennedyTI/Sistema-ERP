services:
  postgres:
    image: postgres:16-alpine
    container_name: portal_industria_postgres
    restart: unless-stopped
    env_file:
      - .env.docker
    environment:
      TZ: ${TZ:-America/Sao_Paulo}
      POSTGRES_DB: ${POSTGRES_DB:-portal_industria_db}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/initdb:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-postgres} -d $${POSTGRES_DB:-portal_industria_db}"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - portal_industria_net

  redis:
    image: redis:8.0-alpine
    container_name: portal_industria_redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - portal_industria_net

  migrations:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portal_industria_migrations
    env_file:
      - .env.docker
    environment:
      TZ: ${TZ:-America/Sao_Paulo}
    depends_on:
      postgres:
        condition: service_healthy
    command: >
      sh -c "python docker/scripts/create_schemas.py
      && alembic -c backend/alembic.ini upgrade head
      && python backend/scripts/seed_printer_alert_rules.py
      && python backend/scripts/seed_printer_snmp_oids.py
      && python manage.py migrate --noinput
      && python manage.py seed_admin_groups"
    volumes:
      - .:/app
    networks:
      - portal_industria_net
    restart: "no"

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portal_industria_api
    restart: unless-stopped
    env_file:
      - .env.docker
    environment:
      TZ: ${TZ:-America/Sao_Paulo}
    depends_on:
      postgres:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    command: uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    networks:
      - portal_industria_net

  admin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portal_industria_admin
    restart: unless-stopped
    env_file:
      - .env.docker
    environment:
      TZ: ${TZ:-America/Sao_Paulo}
    depends_on:
      postgres:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    command: python manage.py runserver 0.0.0.0:8001
    ports:
      - "8001:8001"
    volumes:
      - .:/app
    networks:
      - portal_industria_net

  celery-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portal_industria_celery_worker
    restart: unless-stopped
    user: "10001:10001"
    env_file:
      - .env.docker
    environment:
      TZ: ${TZ:-America/Sao_Paulo}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    command: >
      celery -A backend.app.core.celery_app.celery_app worker
      --loglevel=info
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "celery -A backend.app.core.celery_app.celery_app inspect ping --timeout=5 | grep -q pong",
        ]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 20s
    volumes:
      - .:/app
    networks:
      - portal_industria_net

  celery-beat:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portal_industria_celery_beat
    restart: unless-stopped
    user: "10001:10001"
    env_file:
      - .env.docker
    environment:
      TZ: ${TZ:-America/Sao_Paulo}
    depends_on:
      redis:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
      celery-worker:
        condition: service_healthy
    command: >
      celery -A backend.app.core.celery_app.celery_app beat
      --loglevel=info
      --schedule=/tmp/celerybeat-schedule
    volumes:
      - .:/app
    networks:
      - portal_industria_net

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: portal_industria_frontend
    restart: unless-stopped
    env_file:
      - .env.docker
    environment:
      TZ: ${TZ:-America/Sao_Paulo}
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api}
      VITE_ADMIN_URL: ${VITE_ADMIN_URL:-/admin/}
    depends_on:
      api:
        condition: service_started
    command: npm run dev -- --host 0.0.0.0 --port 5173
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app/frontend
      - frontend_node_modules:/app/frontend/node_modules
    networks:
      - portal_industria_net

  sistema_erp_proxy:
    image: nginx:1.27-alpine
    container_name: sistema_erp_proxy
    restart: unless-stopped
    env_file:
      - .env.docker
    environment:
      NGINX_SERVER_NAME: ${NGINX_SERVER_NAME:-localhost}
      NGINX_HTTPS_PORT: ${NGINX_HTTPS_PORT:-8443}
      SSL_CERT_PATH: ${SSL_CERT_PATH:-/etc/nginx/certs/localhost.crt}
      SSL_KEY_PATH: ${SSL_KEY_PATH:-/etc/nginx/certs/localhost.key}
    depends_on:
      frontend:
        condition: service_started
      api:
        condition: service_started
      admin:
        condition: service_started
    ports:
      - "${NGINX_HTTP_PORT:-8080}:80"
      - "${NGINX_HTTPS_PORT:-8443}:443"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./docker/nginx/conf.d/sistema_erp.conf:/etc/nginx/templates/sistema_erp.conf.template:ro
      - ./docker/nginx/certs:/etc/nginx/certs:ro
    networks:
      - portal_industria_net

networks:
  portal_industria_net:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  frontend_node_modules:
